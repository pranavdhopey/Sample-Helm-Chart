{{- if .Values.internal_ingress.enabled }}
{{- $fullName := include "sampleapp.fullname" . -}}
{{- if and .Values.internal_ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.internal_ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.internal_ingress.annotations "kubernetes.io/ingress.class" .Values.internal_ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ .Values.internal_ingress.name }}
  labels:
    {{- include "sampleapp.labels" . | nindent 4 }}
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.internal_ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.internal_ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.internal_ingress.className }}
  {{- end }}
  {{- if .Values.internal_ingress.tls }}
  tls:
    {{- range .Values.internal_ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- toYaml .Values.internal_ingress.hosts | nindent 2 }}
{{- end }}
