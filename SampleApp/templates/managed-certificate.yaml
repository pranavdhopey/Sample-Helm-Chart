{{- if .Values.managedCertificateEnabled }}
{{- range .Values.managedCertificate }}
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: {{ .name }} 
spec:
  domains:
    {{- range .domains }}
    - {{ . | quote }}
    {{- end }}
---    
{{- end }}    
{{- end }} 